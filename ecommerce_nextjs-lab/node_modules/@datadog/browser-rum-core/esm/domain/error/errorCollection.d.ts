import type { Context, ClocksState } from '@datadog/browser-core';
import type { RumConfiguration } from '../configuration';
import type { LifeCycle } from '../lifeCycle';
import type { CommonContext } from '../contexts/commonContext';
import type { PageStateHistory } from '../contexts/pageStateHistory';
export interface ProvidedError {
    startClocks: ClocksState;
    error: unknown;
    context?: Context;
    handlingStack: string;
    componentStack?: string;
}
export declare function startErrorCollection(lifeCycle: LifeCycle, configuration: RumConfiguration, pageStateHistory: PageStateHistory): {
    addError: ({ error, handlingStack, componentStack, startClocks, context: customerContext }: ProvidedError, savedCommonContext?: CommonContext) => void;
};
export declare function doStartErrorCollection(lifeCycle: LifeCycle, pageStateHistory: PageStateHistory): {
    addError: ({ error, handlingStack, componentStack, startClocks, context: customerContext }: ProvidedError, savedCommonContext?: CommonContext) => void;
};
